import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function FilesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Files</CardTitle>
        <CardDescription>File browsing and text editing land here.</CardDescription>
      </CardHeader>
    </Card>
  );
}
