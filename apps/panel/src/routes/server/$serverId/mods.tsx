import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function ModsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Mods</CardTitle>
        <CardDescription>Installed mods, Modrinth browsing, bulk removal, and modpack flows land here.</CardDescription>
      </CardHeader>
    </Card>
  );
}
