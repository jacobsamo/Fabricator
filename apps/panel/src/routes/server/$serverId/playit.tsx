import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function PlayitPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>playit.gg</CardTitle>
        <CardDescription>Tunnel status and daemon controls land here.</CardDescription>
      </CardHeader>
    </Card>
  );
}
