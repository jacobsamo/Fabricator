import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function BackupsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Backups</CardTitle>
        <CardDescription>Backup configs, snapshots, quick backups, imports, and restores land here.</CardDescription>
      </CardHeader>
    </Card>
  );
}
